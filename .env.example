# ============================================
# Shipi18n Configuration - Only ONE key needed!
# ============================================
# Get your free API key at https://shipi18n.com
# Sign up takes 30 seconds, no credit card required!

# Your Shipi18n API key (starts with sk_live_)
# Used by Server Components, API routes, and middleware
# SECURE: Never exposed to the browser
SHIPI18N_API_KEY=sk_live_your_api_key_here

# API URL (optional - defaults to production)
# SHIPI18N_API_URL=https://x9527l3blg.execute-api.us-east-1.amazonaws.com

# ============================================
# ⚠️  DO NOT USE NEXT_PUBLIC_ VARIABLES
# ============================================
# The NEXT_PUBLIC_ prefix exposes variables to the browser.
# This means your API key would be visible in the page source!
#
# Instead, use the /api/translate route pattern (already set up in this example):
#   Browser → /api/translate → Shipi18n API
#
# This keeps your API key secure on the server.
